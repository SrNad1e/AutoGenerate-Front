overwrite: true
schema: "${API_URL}/graphql"
documents: "src/graphql/**/*.graphql"
generates:
  src/graphql/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typed-document-node"
