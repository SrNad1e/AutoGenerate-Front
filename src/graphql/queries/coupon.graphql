query coupon($input: FiltersCouponInput!) {
  coupon(filtersCouponInput: $input) {
    _id
    code
    title
    updatedAt
    createdAt
    expiration
    message
    number
    status
    value
  }
}
