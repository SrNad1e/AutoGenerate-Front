query GETREGION($filter: FiltersRegionInput) {
  regions(filtersRegionInput: $filter) {
    docs {
     _id
      city
      country
      dpto
      state
      zone {
        name
        _id
      }
    }
  }
}
