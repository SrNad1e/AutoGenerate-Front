query boxes($input: FiltersBoxesInput) {
  boxes(filtersBoxesInput: $input) {
    totalDocs
    totalPages
    page
    docs {
      _id
      base
      updatedAt
      total
      name
      isMain
    }
  }
}
