query currentUser {
  currentUser {
    _id
    username
    name
    pointOfSale {
      _id
    }
    shop {
      _id
      name
      defaultWarehouse {
        _id
        name
      }
    }
    role {
      name
      permissions {
        action
      }
    }
  }
}