query currentUser {
  currentUser {
    username
    name
    pointOfSale {
      _id
    }
    shop {
      _id
      name
      defaultWarehouse {
        _id
        name
      }
    }
    role {
      name
      permissions {
        action
      }
    }
  }
}